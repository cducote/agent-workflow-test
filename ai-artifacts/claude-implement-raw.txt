diff --git a/src/math.js b/src/math.js
index abc1234..def5678 100644
--- a/src/math.js
+++ b/src/math.js
@@ -63,4 +63,16 @@ function cube(x) {
   return x * x * x;
 }
 
-module.exports = { add, multiply, divide, subtract, square, cube };
+/**
+ * Calculates the modulo (remainder) of dividing the first number by the second number.
+ * @param {number} a - Dividend
+ * @param {number} b - Divisor
+ * @returns {number} The remainder of a divided by b, or 0 if either is null/undefined
+ * @throws {Error} If divisor is zero
+ */
+function modulo(a, b) {
+  if (a == null || b == null) {
+    return 0;
+  }
+  if (b === 0) {
+    throw new Error("Division by zero");
+  }
+  return a % b;
+}
+
+module.exports = { add, multiply, divide, subtract, square, cube, modulo };

diff --git a/frontend/lib/math.ts b/frontend/lib/math.ts
index abc1234..def5678 100644
--- a/frontend/lib/math.ts
+++ b/frontend/lib/math.ts
@@ -47,3 +47,15 @@ export function cube(x: number): number | null {
   }
   return x * x * x;
 }
+
+export function modulo(a: number, b: number): number {
+  if (a == null || b == null) {
+    return 0;
+  }
+  if (b === 0) {
+    throw new Error("Division by zero");
+  }
+  return a % b;
+}

diff --git a/frontend/store/calculatorStore.ts b/frontend/store/calculatorStore.ts
index abc1234..def5678 100644
--- a/frontend/store/calculatorStore.ts
+++ b/frontend/store/calculatorStore.ts
@@ -1,7 +1,7 @@
 import { create } from "zustand";
-import { add, subtract, multiply, divide, square, cube } from "@/lib/math";
+import { add, subtract, multiply, divide, square, cube, modulo } from "@/lib/math";
 
-export type Operation = "+" | "-" | "×" | "÷" | "x²" | "x³" | null;
+export type Operation = "+" | "-" | "×" | "÷" | "%" | "x²" | "x³" | null;
 
 export interface HistoryItem {
   id: string;
@@ -139,6 +139,13 @@ export const useCalculatorStore = create<CalculatorState>((set, get) => ({
           }
           result = divide(prev, curr);
           break;
+        case "%":
+          if (curr === 0) {
+            set({ error: "Cannot divide by zero" });
+            return;
+          }
+          result = modulo(prev, curr);
+          break;
         default:
           return;
       }

diff --git a/frontend/components/OperationButtons.tsx b/frontend/components/OperationButtons.tsx
index abc1234..def5678 100644
--- a/frontend/components/OperationButtons.tsx
+++ b/frontend/components/OperationButtons.tsx
@@ -9,6 +9,7 @@ export default function OperationButtons() {
   const operations: { label: string; op: Operation }[] = [
     { label: "÷", op: "÷" },
     { label: "×", op: "×" },
+    { label: "%", op: "%" },
     { label: "-", op: "-" },
     { label: "+", op: "+" },
     { label: "x²", op: "x²" },

diff --git a/tests/math.test.js b/tests/math.test.js
index abc1234..def5678 100644
--- a/tests/math.test.js
+++ b/tests/math.test.js
@@ -1,4 +1,4 @@
-const { add, multiply, divide, subtract, square, cube } = require('../src/math');
+const { add, multiply, divide, subtract, square, cube, modulo } = require('../src/math');
 
 describe('Math functions', () => {
   describe('add', () => {
@@ -140,4 +140,44 @@ describe('Math functions', () => {
       expect(cube(undefined)).toBe(null);
     });
   });
+
+  describe('modulo', () => {
+    it('should return remainder of 10 % 3 as 1', () => {
+      expect(modulo(10, 3)).toBe(1);
+    });
+
+    it('should return remainder of 15 % 4 as 3', () => {
+      expect(modulo(15, 4)).toBe(3);
+    });
+
+    it('should return 0 when dividend is divisible by divisor', () => {
+      expect(modulo(12, 3)).toBe(0);
+    });
+
+    it('should handle negative dividend', () => {
+      expect(modulo(-10, 3)).toBe(-1);
+    });
+
+    it('should handle negative divisor', () => {
+      expect(modulo(10, -3)).toBe(1);
+    });
+
+    it('should handle both negative numbers', () => {
+      expect(modulo(-10, -3)).toBe(-1);
+    });
+
+    it('should throw error on modulo by zero', () => {
+      expect(() => modulo(10, 0)).toThrow('Division by zero');
+    });
+
+    it('should return 0 when first parameter is null', () => {
+      expect(modulo(null, 3)).toBe(0);
+    });
+
+    it('should return 0 when second parameter is undefined', () => {
+      expect(modulo(10, undefined)).toBe(0);
+    });
+  });
 });

diff --git a/frontend/__tests__/math.test.ts b/frontend/__tests__/math.test.ts
index abc1234..def5678 100644
--- a/frontend/__tests__/math.test.ts
+++ b/frontend/__tests__/math.test.ts
@@ -1,4 +1,4 @@
-import { add, subtract, multiply, divide, square, cube } from "@/lib/math";
+import { add, subtract, multiply, divide, square, cube, modulo } from "@/lib/math";
 
 describe("Math functions", () => {
   describe("add", () => {
@@ -109,4 +109,42 @@ describe("Math functions", () => {
       expect(cube(null as any)).toBeNull();
     });
   });
+
+  describe("modulo", () => {
+    it("should return remainder of division", () => {
+      expect(modulo(10, 3)).toBe(1);
+    });
+
+    it("should return 0 when dividend is divisible by divisor", () => {
+      expect(modulo(15, 5)).toBe(0);
+    });
+
+    it("should handle negative dividend", () => {
+      expect(modulo(-10, 3)).toBe(-1);
+    });
+
+    it("should handle negative divisor", () => {
+      expect(modulo(10, -3)).toBe(1);
+    });
+
+    it("should handle both negative numbers", () => {
+      expect(modulo(-10, -3)).toBe(-1);
+    });
+
+    it("should throw error on modulo by zero", () => {
+      expect(() => modulo(5, 0)).toThrow("Division by zero");
+    });
+
+    it("should handle null values", () => {
+      expect(modulo(null as any, 3)).toBe(0);
+      expect(modulo(5, null as any)).toBe(0);
+    });
+
+    it("should handle decimal numbers", () => {
+      expect(modulo(7.5, 2.5)).toBe(0);
+    });
+
+    it("should handle floating point precision", () => {
+      expect(modulo(10.7, 3.2)).toBeCloseTo(1.1);
+    });
+  });
 });